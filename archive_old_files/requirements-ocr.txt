# Optional OCR dependencies for scanned/image-based PDFs
# Install only when you want local OCR mode.
# macOS system dependency:
#   brew install tesseract poppler
# Python dependencies:
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.17.0
